generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider          = "mysql"
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

model User {
  id           Int           @id @default(autoincrement())
  email        String        @unique
  password     String?
  rsvp         Boolean
  hackerApp    HackerApp?
  volunteerApp VolunteerApp?
}

model HackerApp {
  firstName String
  lastName  String
  phone     String
  age       Int
  yog       Int
  school    String
  country   String
  diet      Diet[]
  shirt     String
  outreach  String
  user      User   @relation(fields: [userId], references: [id])
  userId    Int    @unique
}

model VolunteerApp {
  firstName String
  lastName  String
  phone     String
  age       Int
  role      String // volunteer, mentor, judge, sponsor
  diet      Diet[]
  shirt     String
  user      User   @relation(fields: [userId], references: [id])
  userId    Int    @unique
}

model Diet {
  diet               String
  hackerApp          HackerApp?    @relation(fields: [hackerAppUserId], references: [userId])
  hackerAppUserId    Int           @unique
  volunteerApp       VolunteerApp? @relation(fields: [volunteerAppUserId], references: [userId])
  volunteerAppUserId Int           @unique
}
